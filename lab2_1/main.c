#include <stdio.h>
#include <stdlib.h>

int main()
{
    const float s = 9.8;
    int pool = 98;
    float money = 61.5243;
    printf(" s = %f (float)\n pool = %d (int)\n money = %f",s,pool,money);
    return 0;
}
