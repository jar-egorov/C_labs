#include <stdio.h>
#include <stdlib.h>
int main()
{
    const float s = 9.8;
    int pool = 98;
    float money = 61.5243;
    printf("s = %lf (float)\npool = %d(int)\nmoney = %lf(float)\n",s,pool,money);
    return 0;
}
